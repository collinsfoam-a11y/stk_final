interface:
  display_name: "Stk Final"
  short_description: "Repo-specific patterns and workflows for stk_final"
  default_prompt: "Use the stk_final repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true